/**
 * This was originally supposed to store book info inside database instead of simple file storage.
 * That proved to be more difficult than expected, so this is currently dead code. Should migrate
 * to SQLite storage at some point.
 */
package rs.lukaj.android.stories.io.database; //todo